include:
  - local-supabase-project/docker-compose.yml

services:
  capstone-web:
    image: ghcr.io/ecsecapstone/capstone-project-2025-team_19:dev
    restart: on-failure:5
    ports:
      - "3001:3001"
    env_file:
      - .env
    depends_on:
      - studio
      - kong
      - auth
      - rest
      - realtime
      - storage
      - imgproxy
      - meta
      - functions
      - analytics
      - db
      - vector
      - supavisor
